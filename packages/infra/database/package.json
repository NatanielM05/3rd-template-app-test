{
  "name": "@app/database",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo '⚠️  Database: No dev mode'",
    "build": "echo '⚠️  Database: AI will create src/ and add build script'",
    "test": "echo '⚠️  Database: AI will add tests'",
    "test:init": "echo '⚠️  Database: AI must implement test:init script (e.g.: tsc && node dist/test-init.js)'"
  },
  "dependencies": {
    "lowdb": "^7.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}