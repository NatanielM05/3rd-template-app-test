{
  "name": "@app/openapi",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "echo '⚠️  OpenAPI: No dev mode'",
    "build": "redocly bundle openapi.yaml -o dist/openapi.yaml",
    "validate": "redocly lint openapi.yaml",
    "test": "pnpm validate"
  },
  "devDependencies": {
    "@redocly/cli": "^1.10.0"
  }
}
